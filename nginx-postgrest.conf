server {
    listen 80;
    server_name _;

    location /rest/v1/ {
        proxy_pass http://lokalizacje-postgrest:3000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Connection "";
    }
}
